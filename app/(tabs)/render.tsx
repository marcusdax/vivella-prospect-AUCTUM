import { View, Text } from 'react-native';

export default function RenderScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-parchment">
      <Text className="text-2xl font-serif text-root-earth">Render</Text>
    </View>
  );
}
